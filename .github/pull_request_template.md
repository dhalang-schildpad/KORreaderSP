**What and why**

**Checked**
- [ ] `python3 -m unittest discover -s generator/tests`
- [ ] `python3 generator/validate.py` on affected puzzles
- [ ] `python3 generator/clues.py check` on affected batches
- [ ] `luacheck arrowword.koplugin` and `tools/test.sh` (plugin changes)

**Clue batches drafted with a language model, if any:**
